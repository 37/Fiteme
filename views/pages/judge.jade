extends ../partials/skeleton

block content

	if user
		h3.judge Judge me
		h4.topic Topic Name: {{topicname}}
		.arguments
			.userOne.first
				img(class="icon left",src="images/icon-profile_02.png")
				p User one first argument there are many things g
			.userTwo.first
				img(class="icon right",src="images/icon-profile_01.png")
				p User two first argument
			.userOne.second
				img(class="icon left",src="images/icon-profile_02.png")
				p User one second argument hdjfk fsahjkf fhsajkfh fhsajkh fhsajkfh  hjfksahfjk  fsahjkf hfs ahjkf h
			.userTwo.second
				img(class="icon right",src="images/icon-profile_01.png")
				p User two second argument dhajsjk dhsajdhsahd jks djkahsjdkahs
			
	else
		.container
			h1  Wanna fite m8?
				p Log in first.
				p
					a.btn.btn-primary(href="/login") Login
				p
					span.small I don't have an account?
					span &nbsp;
					a.small(href="/register") Get 1337.

block scripts
	script(type='text/javascript').
		$(document).ready(function (){
			$("#settings").click(function () {
				var button = $(this);
				if ($(this).hasClass('active')){
					console.log("Button active, resetting!");
					$("#slider").slideUp("500");
					$(".achievementlist").delay(500).slideDown("500");
					$(this).removeClass('active');
					return false;
				} else {
					console.log("Button inactive, opening!");
					$(".achievementlist").slideUp("500");
					$("#slider").delay(500).slideDown("500");
					$(this).addClass('active');
					return false;
				}
			});
		});
